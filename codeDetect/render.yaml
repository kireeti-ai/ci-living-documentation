services:
  - type: web
    name: code-detector
    env: docker
    plan: starter
    region: oregon
    dockerContext: ./codeDetect
    dockerfilePath: ./codeDetect/Dockerfile
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
