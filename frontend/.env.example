# Frontend Environment Variables (for local development)
# This file is NOT used in production - Render sets VITE_API_URL automatically

# API URL - defaults to localhost if not set
VITE_API_URL=http://localhost:8000
