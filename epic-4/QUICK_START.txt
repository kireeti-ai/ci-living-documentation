╔══════════════════════════════════════════════════════════════════════════════╗
║                    EPIC-4 ENVIRONMENT VARIABLES QUICK REFERENCE              ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🏠 LOCAL DEVELOPMENT (Recommended)                                           │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Copy the template:
     $ cp .env.example .env

  2. Edit with your values:
     $ nano .env

  3. Load and run:
     $ export $(cat .env | xargs)
     $ python -m epic4.run --commit abc123

  OR with Docker:
     $ docker-compose up api

┌──────────────────────────────────────────────────────────────────────────────┐
│ ☁️  GITHUB ACTIONS (CI/CD)                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

  Settings → Secrets and variables → Actions → New repository secret

  Add these secrets:
    • R2_ACCOUNT_ID           → Your Cloudflare account ID
    • R2_ACCESS_KEY_ID        → Your R2 access key
    • R2_SECRET_ACCESS_KEY    → Your R2 secret key
    • DOCS_BUCKET_PATH        → r2://bucket-name/path

  ✅ Workflow already configured in: .github/workflows/epic4_workflow.yaml

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔧 TERMINAL EXPORT (Quick Test)                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  $ export R2_ACCOUNT_ID="your_account_id_here"
  $ export R2_ACCESS_KEY_ID="your_access_key_id"
  $ export R2_SECRET_ACCESS_KEY="your_secret_access_key"
  $ export DOCS_BUCKET_PATH="r2://your-bucket/path"
  $ export GITHUB_TOKEN="ghp_your_token"
  $ export REPO_OWNER="your-org"
  $ export REPO_NAME="your-repo"

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📋 REQUIRED VARIABLES                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

  ✅ GITHUB_TOKEN          GitHub API token
  ✅ REPO_OWNER            Repository owner/org
  ✅ REPO_NAME             Repository name
  ✅ COMMIT_SHA            Commit SHA being processed

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📋 OPTIONAL (For Cloudflare R2)                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  ⭕ R2_ACCOUNT_ID          Cloudflare account ID
  ⭕ R2_ACCESS_KEY_ID       R2 API access key
  ⭕ R2_SECRET_ACCESS_KEY   R2 API secret key
  ⭕ DOCS_BUCKET_PATH       r2://bucket-name/path/to/docs

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔐 SECURITY CHECKLIST                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

  ✅ .env file in .gitignore
  ✅ Never commit credentials to git
  ✅ Use CI secrets for automation
  ✅ Rotate credentials every 90 days
  ✅ Use masked variables in GitLab CI

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🆘 QUICK TROUBLESHOOTING                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  Problem: "R2 client not available"
  Solution: Verify all 3 R2 variables are set:
    $ echo $R2_ACCOUNT_ID $R2_ACCESS_KEY_ID $R2_SECRET_ACCESS_KEY

  Problem: Variables not loading
  Solution: Load .env explicitly:
    $ export $(cat .env | xargs)

  Problem: Wrong working directory
  Solution: Navigate to epic-4 root:
    $ cd /Users/kireeti/Downloads/software_project/ci-living-documentation/epic-4/epic-4
    $ ls .env .env.example

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📚 DETAILED GUIDES                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  • ENV_SETUP_GUIDE.md        → Comprehensive environment setup
  • CLOUDFLARE_R2_SETUP.md    → R2-specific configuration
  • .env.example              → Template file
  • README.md                 → General documentation

╔══════════════════════════════════════════════════════════════════════════════╗
║  Need help? See ENV_SETUP_GUIDE.md for platform-specific instructions       ║
╚══════════════════════════════════════════════════════════════════════════════╝
